# model/conda.yaml — SAU KHI SỬA
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow==1.30.0
      - cloudpickle==2.2.0
      - psutil==5.8.0
      - scikit-learn==0.24.1          # giữ nguyên để khớp với model.pkl
      - azureml-inference-server-http # ← thêm: bắt buộc cho inference
      - azureml-ai-monitoring         # ← thêm: bắt buộc cho inference
name: mlflow-env
